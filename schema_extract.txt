model User {
  id               String         @id @default(uuid())
  name             String
  email            String         @unique
  password         String
  createdAt        DateTime       @default(now())
  isActive         Boolean        @default(true)
  role             UserRole       @default(USER)
  updatedAt        DateTime       @updatedAt
  activeBusinessId String?
  employees        Employee?
  memberships      BusinessUser[]
  businessesOwned  Business[]     @relation("BusinessOwner")
  activeBusiness   Business?      @relation("ActiveBusiness", fields: [activeBusinessId], references: [id])
  settings         Settings[]     @relation("SettingsToUser")

  resetToken       String?
  resetTokenExpiry DateTime?

  // Project Operations Relations
  projectManagerOf       Project[]              @relation("ProjectManager")
  assignedTasks          ProjectTask[]          @relation("TaskAssignedTo")
  assignedReqs           ProjectRequirement[]   @relation("RequirementAssignedTo")
  milestonesOwned        ProjectMilestone[]     @relation("MilestoneOwner")
  issuesReported         ProjectIssue[]         @relation("IssueReporter")
  issuesAssigned         ProjectIssue[]         @relation("IssueAssignee")
  changeRequests         ProjectChangeRequest[] @relation("ChangeRequestOwner")
  changeRequestsAssigned ProjectChangeRequest[] @relation("ChangeRequestAssignee")
  documentsUploaded      ProjectDocument[]      @relation("DocumentUploader")
  budgetApprovals        ProjectBudgetHistory[] @relation("BudgetHistoryApprover")

  managedExpenses     Expense[]            @relation("ExpenseManager")
  financeExpenses     Expense[]            @relation("ExpenseFinance")
  projectBudgets      ProjectBudget[]
  ticketComments      TicketComment[]
  resourceAllocations ResourceAllocation[]

  @@map("users")
}

model Business {
  id        String   @id @default(uuid())
  name      String
  ownerId   String
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  country            LegacyCountry @default(INDIA)
  state              String?
  city               String?
  currency           String?
  countryCode        String?
  currencyCode       String?
  currencySymbol     String?
  taxType            String?
  gstNumber          String?
  vatNumber          String?
  financialYearStart DateTime?
  financialYearEnd   DateTime?

  // Enterprise Details
  legalName    String?
  businessType String?
  industry     String?
  description  String? @db.Text
  pinCode      String?
  timeZone     String?
  language     String?
  emirate      String?

  // India Specific
  pan                 String?
  tan                 String?
  msmeNumber          String?
  gstRegistrationType String?

  // UAE Specific
  vatRegistrationDate DateTime?
  vatRegistrationType String?
  //   accounts            Account[]
  activities          Activity[]
  bills               Bill[]
  //   Campaign            Campaign[]
  contracts           Contract[]
  contractTypes       ContractType[]
  creditNotes         CreditNote[]
  //   CrmTask             CrmTask[]
  customerContacts    CustomerContact[]
  deals               Deal[]
  //   EmailLog            EmailLog[]
  employees           Employee[]
  expenses            Expense[]
  journalEntries      JournalEntry[]
  leads               Lead[]
  //   LeadConversionLog   LeadConversionLog[]
  //   Note                Note[]
  overtimes           Overtime[]
  //   products            Product[]
  //   projects            Project[]
  //   PurchaseOrder       PurchaseOrder[]
  quotations          Quotation[]
  salesOrders         SalesOrder[]
  settings            Settings?
  //   timeEntries         TimeEntry[]
  attendances         Attendance[]
  vendors             Vendor[]
  warehouses          Warehouse[]
  //  audit_logs          audit_logs[]
  //  batches             batches[]
  //  brands              brands[]
  users               BusinessUser[]
  owner               User              @relation("BusinessOwner", fields: [ownerId], references: [id])
  //  categories          categories[]
  customers           Customer[]
  //  goods_receive_notes goods_receive_notes[]
  invoices            Invoice[]
  //  notifications       notifications[]
  payments            Payment[]
  //  purchase_requests   purchase_requests[]
  //   purchase_returns    purchase_returns[]
  //   recurring_invoices  recurring_invoices[]
  roles               Role[]
  //   sales_returns       sales_returns[]
  //   serial_numbers      serial_numbers[]
  //   stock_adjustments   stock_adjustments[]
  stockMovements      StockMovement[]
  //   stock_transfers     stock_transfers[]
  subscription        Subscription?
  //   units               units[]
  activeUsers         User[]            @relation("ActiveBusiness")

  // Phase2 relations
  resourceAllocations    ResourceAllocation[]
  Note                   Note[]
  EmailLog               EmailLog[]
  Campaign               Campaign[]
  CrmTask                CrmTask[]
  LeadConversionLog      LeadConversionLog[]
  categories             Category[]
  brands                 Brand[]
  units                  Unit[]
  batches                Batch[]
  serialNumbers          SerialNumber[]
  stockTransfers         StockTransfer[]
  stockAdjustments       StockAdjustment[]
  purchaseRequests       PurchaseRequest[]
  goodsReceiveNotes      GoodsReceiveNote[]
  purchaseReturns        PurchaseReturn[]
  salesReturns           SalesReturn[]
  recurringInvoices      RecurringInvoice[]
  auditLogs              AuditLog[]
  notifications          Notification[]
  accounts               Account[]
  products               Product[]
  purchaseOrders         PurchaseOrder[]
  timeEntries            TimeEntry[]
  inventoryLayers        InventoryLayer[]
  complianceTasks        ComplianceTask[]
  projects               Project[]
  projectRequirements    ProjectRequirement[]
  projectEstimations     ProjectEstimation[]
  projectMilestones      ProjectMilestone[]
  projectTasks           ProjectTask[]
  projectPlannings       ProjectPlanning[]
  projectMasters         ProjectMaster[]
  projectIssues          ProjectIssue[]
  projectChangeRequests  ProjectChangeRequest[]
  projectRisks           ProjectRisk[]
  projectDocuments       ProjectDocument[]
  projectMeetings        ProjectMeeting[]
  projectTemplates       ProjectTemplate[]
  projectBudgetHistories ProjectBudgetHistory[]
  expenseAuditLogs       ExpenseAuditLog[]
  invoiceAuditLogs       InvoiceAuditLog[]
  documentAuditLogs      DocumentAuditLog[]
  projectBudgets         ProjectBudget[]
  warranties             Warranty[]
  warrantyAttachments    WarrantyAttachment[]
  warrantyReminders      WarrantyReminder[]
  amcs                   AMC[]
  amcVisits              AMCVisit[]
  amcRenewals            AMCRenewal[]
  tickets                Ticket[]
  ticketComments         TicketComment[]
  ticketAttachments      TicketAttachment[]

  // Phase 1 Multi-Country / Multi-Currency Additions
  countryId      String?
  baseCurrencyId String?
  taxFrameworkId String?

  countryRef         Country?            @relation(fields: [countryId], references: [id])
  baseCurrency       Currency?           @relation("BusinessBaseCurrency", fields: [baseCurrencyId], references: [id])
  taxFramework       TaxFramework?       @relation(fields: [taxFrameworkId], references: [id])
  taxTransactions    TaxTransaction[]
  taxRules           TaxRule[]
  taxAccountMappings TaxAccountMapping[]

  @@map("businesses")
}

model Customer {
  id String @id @default(uuid())

  //////////////////////////////////////////////////////
  // BUSINESS RELATION (REPLACE TENANT)
  //////////////////////////////////////////////////////
  businessId String
  business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  //////////////////////////////////////////////////////
  // CUSTOMER DETAILS
  //////////////////////////////////////////////////////
  company         String
  vatNumber       String?
  phone           String?
  website         String?
  group           String?
  currency        String  @default("SYSTEM")
  defaultLanguage String  @default("SYSTEM")

  //////////////////////////////////////////////////////
  // MAIN ADDRESS
  //////////////////////////////////////////////////////
  address String?
  city    String?
  state   String?
  zipCode String?
  country String?

  //////////////////////////////////////////////////////
  // BILLING ADDRESS
  //////////////////////////////////////////////////////
  billingStreet  String?
  billingCity    String?
  billingState   String?
  billingZipCode String?
  billingCountry String?

  //////////////////////////////////////////////////////
  // SHIPPING ADDRESS
  //////////////////////////////////////////////////////
  shippingStreet  String?
  shippingCity    String?
  shippingState   String?
  shippingZipCode String?
  shippingCountry String?
  region          Region

  //////////////////////////////////////////////////////
  // UPGRADED CRM ACCOUNT FIELDS
  //////////////////////////////////////////////////////
  industry        String?
  annualRevenue   Float?
  employeeCount   Int?
  accountOwnerId  String?
  accountOwner    BusinessUser?   @relation("CustomerOwner", fields: [accountOwnerId], references: [id])
  accountType     CrmAccountType?
  parentAccountId String?
  parentAccount   Customer?       @relation("CustomerParentChild", fields: [parentAccountId], references: [id], onDelete: SetNull)
  childAccounts   Customer[]      @relation("CustomerParentChild")
  linkedinUrl     String?
  facebookUrl     String?
  twitterUrl      String?
  tags            String[]
  description     String?
  lastContactedAt DateTime?
  crmStatus       CrmStatus?
  isDeleted       Boolean         @default(false)
  deletedAt       DateTime?

  //////////////////////////////////////////////////////
  // STATUS + META
  //////////////////////////////////////////////////////
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  //////////////////////////////////////////////////////
  // RELATIONS
  //////////////////////////////////////////////////////
  invoices            Invoice[]
  payments            Payment[]
  leadId              String?              @unique
  leads               Lead[]
  inquiries           Lead[]               @relation("LeadToCustomer")
  creditNotes         CreditNote[]
  customerContacts    CustomerContact[]
  contracts           Contract[]
  deals               Deal[]
  activities          Activity[]
  quotations          Quotation[]
  salesOrders         SalesOrder[]
  projects            Project[]
  projectRequirements ProjectRequirement[]
  notes               Note[]
  emailLogs           EmailLog[]
  crmTasks            CrmTask[]
  leadConversionLogs  LeadConversionLog[]
  salesReturns        SalesReturn[]
  recurringInvoices   RecurringInvoice[]
  projectMeetings     ProjectMeeting[]
  projectBudgets      ProjectBudget[]
  warranties          Warranty[]
  amcs                AMC[]
  tickets             Ticket[]
  expenses            Expense[]

  @@index([businessId])
  @@index([accountOwnerId])
  @@index([parentAccountId])
  @@index([isDeleted])
  @@map("customers")
}

model Vendor {
  id String @id @default(uuid())

  //////////////////////////////////////////////////////
  // BUSINESS
  //////////////////////////////////////////////////////
  businessId String
  business   Business @relation(fields: [businessId], references: [id])

  //////////////////////////////////////////////////////
  // BASIC INFO
  //////////////////////////////////////////////////////
  vendorCode    String?
  name          String
  vendorType    String?
  contactPerson String?
  email         String?
  countryCode   String?
  phone         String?

  //////////////////////////////////////////////////////
  // COMPANY DETAILS (Legacy fields kept for backward compatibility)
  //////////////////////////////////////////////////////
  companyName String?
  vatNumber   String?
  website     String?

  //////////////////////////////////////////////////////
  // ADDRESS
  //////////////////////////////////////////////////////
  address String?
  city    String?
  state   String?
  zipCode String?
  country String?

  //////////////////////////////////////////////////////
  // STATUS
  //////////////////////////////////////////////////////
  isActive        Boolean @default(true)
  status          String  @default("ACTIVE") // ACTIVE, INACTIVE
  preferredVendor Boolean @default(false)

  //////////////////////////////////////////////////////
  // DETAILS
  //////////////////////////////////////////////////////
  taxRegistrationNumber String?
  paymentTerms          String?
  currency              String?
  taxNumber             String?
  openingBalance        Float   @default(0)
  balance               Float   @default(0)
  creditLimit           Float?
  notes                 String? @db.Text

  //////////////////////////////////////////////////////
  // META
  //////////////////////////////////////////////////////
  createdBy String?
  updatedBy String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  //////////////////////////////////////////////////////
  // RELATIONS
  //////////////////////////////////////////////////////
  expenses          Expense[]
  purchaseOrders    PurchaseOrder[]
  bills             Bill[]
  goodsReceiveNotes GoodsReceiveNote[]
  purchaseReturns   PurchaseReturn[]
  creditNotes       CreditNote[]
}

model Product {
  id         String @id @default(uuid())
  businessId String

  name        String
  description String?
  sku         String  @unique
  barcode     String?

  price     Float
  costPrice Float

  type       ItemType
  taxCode    String? // HSN or SAC
  taxPercent Float    @default(0)

  unit     String  @default("pcs")
  isActive Boolean @default(true)

  categoryId String?
  brandId    String?
  unitId     String?

  reorderLevel Float @default(0)
  minimumStock Float @default(0)
  openingStock Float @default(0)

  isBatchTracking  Boolean @default(false)
  isSerialTracking Boolean @default(false)

  image       String?
  attachments Json?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  stock       Stock[]
  category    Category? @relation(fields: [categoryId], references: [id], onDelete: SetNull)
  brand       Brand?    @relation(fields: [brandId], references: [id], onDelete: SetNull)
  productUnit Unit?     @relation(fields: [unitId], references: [id], onDelete: SetNull)

  stockMovements        StockMovement[]
  batches               Batch[]
  serialNumbers         SerialNumber[]
  stockTransferItems    StockTransferItem[]
  stockAdjustmentItems  StockAdjustmentItem[]
  purchaseRequestItems  PurchaseRequestItem[]
  purchaseOrderItems    PurchaseOrderItem[]
  billItems             BillItem[]
  goodsReceiveNoteItems GoodsReceiveNoteItem[]
  purchaseReturnItems   PurchaseReturnItem[]

  business              Business               @relation(fields: [businessId], references: [id])
  quotationItems        QuotationItem[]
  salesOrderItems       SalesOrderItem[]
  invoiceItems          InvoiceItem[]
  salesReturnItems      SalesReturnItem[]
  recurringInvoiceItems RecurringInvoiceItem[]
  inventoryLayers       InventoryLayer[]

  @@index([businessId])
}

model Lead {
  id         String   @id @default(uuid())
  businessId String
  business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  name       String
  position   String?
  email      String?
  phone      String?

  company String?
  website String?

  city    String?
  state   String?
  country String?
  zipCode String?

  status LeadStatus @default(NEW)
  source LeadSource

  assignedToId String?
  assignedTo   BusinessUser? @relation(fields: [assignedToId], references: [id])
  tags         String[]
  leadValue    Float?

  description     String?
  isPublic        Boolean @default(false)
  contactedToday  Boolean @default(false)
  defaultLanguage String?

  stageId String?
  stage   PipelineStage? @relation(fields: [stageId], references: [id])

  leadActivities LeadActivity[]
  leadNotes      LeadNote[]
  tasks          LeadTask[]
  reminders      LeadReminder[]

  //////////////////////////////////////////////////////
  // NEW ENTERPRISE INQUIRY FIELDS
  //////////////////////////////////////////////////////
  inquiryNumber        String?
  inquiryTitle         String?
  inquiryType          String?
  priority             LeadPriority @default(MEDIUM)
  department           String?
  businessUnit         String?
  currency             String?
  budgetRange          String?
  expectedRevenue      Float?
  probability          Int?
  expectedDecisionDate DateTime?

  projectType            String?
  executionType          ExecutionType @default(AUTO_DETECT)
  expectedStartDate      DateTime?
  expectedCompletionDate DateTime?
  expectedDuration       String?
  businessRequirement    String?       @db.Text
  currentBusinessProblem String?       @db.Text
  expectedSolution       String?       @db.Text
  scopeSummary           String?       @db.Text
  deliverables           String?       @db.Text
  estimatedTeamSize      Int?
  timezone               String?

  internalNotes   String? @db.Text
  salesStrategy   String? @db.Text
  competitors     String?
  competitorName  String?
  riskLevel       String?
  winProbability  Int?
  expectedProfit  Float?
  expectedMargin  Float?
  managementNotes String? @db.Text

  nextFollowUpDate DateTime?
  reminder         Boolean   @default(false)
  meetingDate      DateTime?
  meetingType      String?
  meetingLocation  String?
  followUpNotes    String?   @db.Text

  companySize            String?
  preferredCommunication String?
  industry               String?
  gstVatNumber           String?

  //////////////////////////////////////////////////////
  // CRM LEAD UPGRADES
  //////////////////////////////////////////////////////
  score      Int       @default(0)
  isDeleted  Boolean   @default(false)
  deletedAt  DateTime?
  campaignId String?
  campaign   Campaign? @relation(fields: [campaignId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  customerId String?
  customer   Customer? @relation("LeadToCustomer", fields: [customerId], references: [id])

  requirementId String?
  requirement   ProjectRequirement? @relation(fields: [requirementId], references: [id])

  customers Customer[]

  activities    Activity[]
  notes         Note[]
  emailLogs     EmailLog[]
  crmTasks      CrmTask[]
  conversionLog LeadConversionLog?

  @@index([businessId])
  @@index([assignedToId])
  @@index([stageId])
  @@index([isDeleted])
  @@index([campaignId])
}

model Deal {
  id                String           @id @default(uuid())
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  businessId        String
  business          Business         @relation(fields: [businessId], references: [id], onDelete: Cascade)
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  name              String
  amount            Float
  currency          String           @default("INR")
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  customerId        String
  customer          Customer         @relation(fields: [customerId], references: [id])
  contactId         String?
  contact           CustomerContact? @relation(fields: [contactId], references: [id])
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  stage             String           @default("New")
  probability       Int?
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  expectedCloseDate DateTime?

  //////////////////////////////////////////////////////
  // CRM DEAL PIPELINE UPGRADES
  //////////////////////////////////////////////////////
  status       DealStatus         @default(OPEN)
  lostReason   String?
  stageHistory DealStageHistory[]
  campaignId   String?
  campaign     Campaign?          @relation(fields: [campaignId], references: [id], onDelete: SetNull)
  isDeleted    Boolean            @default(false)
  deletedAt    DateTime?

  //////////////////////////////////////////////////////
  // META
  //////////////////////////////////////////////////////
  assignedToId String?
  assignedTo   BusinessUser? @relation(fields: [assignedToId], references: [id])
  source       String?
  description  String?

  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
  activities  Activity[]
  quotations  Quotation[]
  salesOrders SalesOrder[]
  notes       Note[]
  emailLogs   EmailLog[]
  crmTasks    CrmTask[]

  @@index([businessId])
  @@index([customerId])
  @@index([contactId])
  @@index([assignedToId])
  @@index([campaignId])
  @@index([isDeleted])
}

model Quotation {
  id          String   @id @default(uuid())
  businessId  String
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  quoteNumber String
  title       String?

  customerId String
  customer   Customer @relation(fields: [customerId], references: [id])

  contactId String?
  contact   CustomerContact? @relation(fields: [contactId], references: [id])

  dealId String?
  deal   Deal?   @relation(fields: [dealId], references: [id])

  assignedToId String?
  assignedTo   BusinessUser? @relation(fields: [assignedToId], references: [id])

  status QuotationStatus @default(DRAFT)

  subtotal    Float @default(0)
  tax         Float @default(0)
  discount    Float @default(0)
  totalAmount Float @default(0)

  currency        String  @default("INR")
  termsConditions String?

  // Regional Taxation & Shipping Fields
  reverseCharge    Boolean?  @default(false)
  shippingCharges  Float     @default(0)
  cgst             Float?    @default(0)
  sgst             Float?    @default(0)
  igst             Float?    @default(0)
  tds              Float?    @default(0)
  vatAmount        Float?    @default(0)
  vatPercentage    Float?    @default(0)
  vatType          String?
  emirate          String?
  ewayBillNo       String?
  transportDetails String?
  issueDate        DateTime  @default(now())
  expiryDate       DateTime?
  notes            String?

  isDeleted Boolean   @default(false)
  deletedAt DateTime?

  items       QuotationItem[]
  salesOrders SalesOrder[]
  invoices    Invoice[]
  payments    Payment[]

  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  projects     Project[]
  exchangeRate Float?    @default(1.0)

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("QuotationTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("QuotationBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  baseCurrencyAmount    Float?

  @@unique([businessId, quoteNumber])
  @@index([customerId])
  @@index([dealId])
  @@index([isDeleted])
}

model SalesOrder {
  id          String   @id @default(uuid())
  businessId  String
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  orderNumber String

  customerId String
  customer   Customer @relation(fields: [customerId], references: [id])

  contactId String?
  contact   CustomerContact? @relation(fields: [contactId], references: [id])

  quotationId String?
  quotation   Quotation? @relation(fields: [quotationId], references: [id])

  dealId String?
  deal   Deal?   @relation(fields: [dealId], references: [id])

  assignedToId String?
  assignedTo   BusinessUser? @relation(fields: [assignedToId], references: [id])

  status SalesOrderStatus @default(DRAFT)

  subtotal    Float @default(0)
  tax         Float @default(0)
  discount    Float @default(0)
  totalAmount Float @default(0)

  currency        String  @default("INR")
  termsConditions String?

  // Regional Taxation & Shipping Fields
  reverseCharge        Boolean?  @default(false)
  shippingCharges      Float     @default(0)
  cgst                 Float?    @default(0)
  sgst                 Float?    @default(0)
  igst                 Float?    @default(0)
  tds                  Float?    @default(0)
  vatAmount            Float?    @default(0)
  vatPercentage        Float?    @default(0)
  vatType              String?
  emirate              String?
  ewayBillNo           String?
  transportDetails     String?
  customerReference    String?
  deliveryInstructions String?
  paymentTerms         String?
  shippingMethod       String?
  placeOfSupply        String?
  orderDate            DateTime  @default(now())
  deliveryDate         DateTime?
  notes                String?

  isDeleted Boolean   @default(false)
  deletedAt DateTime?

  items        SalesOrderItem[]
  invoices     Invoice[]
  salesReturns SalesReturn[]

  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  projects     Project[]
  exchangeRate Float?    @default(1.0)

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("SalesOrderTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("SalesOrderBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  baseCurrencyAmount    Float?

  @@unique([businessId, orderNumber])
  @@index([customerId])
  @@index([quotationId])
  @@index([isDeleted])
}

model Invoice {
  id            String        @id @default(uuid())
  businessId    String
  customerId    String
  invoiceNumber String
  status        InvoiceStatus @default(UNPAID)
  invoiceDate   DateTime      @default(now())
  dueDate       DateTime?
  currency      String        @default("AED")
  poNumber      String?
  poDate        DateTime?
  soNumber      String?
  soDate        DateTime?

  //NOTE//
  adminNote String?
  terms     String?

  // Regional Taxation & Shipping Fields
  reverseCharge    Boolean? @default(false)
  shippingCharges  Float    @default(0)
  cgst             Float?   @default(0)
  sgst             Float?   @default(0)
  igst             Float?   @default(0)
  tds              Float?   @default(0)
  vatAmount        Float?   @default(0)
  vatPercentage    Float?   @default(0)
  vatType          String?
  emirate          String?
  ewayBillNo       String?
  transportDetails String?

  //totalAmount//
  subtotal         Float  @default(0)
  totalTax         Float  @default(0)
  discount         Float  @default(0)
  grandTotal       Float  @default(0)
  amountPaid       Float  @default(0)
  totalCogs        Float? @default(0)
  totalGrossProfit Float? @default(0)

  //Business//
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  pdfUrl   String?
  business Business @relation(fields: [businessId], references: [id])
  customer Customer @relation(fields: [customerId], references: [id])

  contactId String?
  contact   CustomerContact? @relation(fields: [contactId], references: [id])

  quotationId String?
  quotation   Quotation? @relation(fields: [quotationId], references: [id])

  salesOrderId   String?
  designTemplate String        @default("modern")
  salesOrder     SalesOrder?   @relation(fields: [salesOrderId], references: [id])
  items          InvoiceItem[]

  projectId String?
  project   Project? @relation(fields: [projectId], references: [id])

  isDeleted Boolean   @default(false)
  deletedAt DateTime?

  auditLogs          InvoiceAuditLog[]
  salesReturns       SalesReturn[]
  payments           Payment[]
  creditNotes        CreditNote[]
  paymentAllocations PaymentAllocation[]
  exchangeRate       Float?              @default(1.0)

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("InvoiceTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("InvoiceBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  baseCurrencyAmount    Float?

  @@unique([businessId, invoiceNumber]) // invoice numbers unique per business
  @@index([businessId])
  @@index([businessId, invoiceDate])
  @@index([businessId, status])
  @@map("invoices")
}

model Payment {
  id                 String              @id @default(uuid())
  invoiceId          String?
  quotationId        String?
  customerId         String? // 🔥 Added for unassigned payments / advances
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  businessId         String
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  amount             Float
  amountAllocated    Float               @default(0)
  status             String              @default("unapplied")
  paymentDate        DateTime
  paymentMode        String
  transactionId      String?
  note               String?
  createdAt          DateTime            @default(now())
  /// ///////////////////////////////////////////////////
  /// ///////////////////////////////////////////////////
  createdBy          String
  pdfUrl             String?
  //   creditNoteId  String?
  paymentNumber      String
  billId             String?
  projectId          String?
  bill               Bill?               @relation(fields: [billId], references: [id])
  business           Business            @relation(fields: [businessId], references: [id])
  customer           Customer?           @relation(fields: [customerId], references: [id]) // 🔥 Relation
  project            Project?            @relation(fields: [projectId], references: [id])
  //   creditNote    CreditNote? @relation(fields: [creditNoteId], references: [id])
  invoice            Invoice?            @relation(fields: [invoiceId], references: [id])
  quotation          Quotation?          @relation(fields: [quotationId], references: [id])
  paymentAllocations PaymentAllocation[]
  currency           String?             @default("AED")
  exchangeRate       Float?              @default(1.0)

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("PaymentTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("PaymentBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  baseCurrencyAmount    Float?

  @@index([businessId, paymentDate, transactionCurrencyId])
  @@index([invoiceId])
  @@index([customerId])
  @@index([businessId, paymentDate])
  @@map("payments")
}

model CreditNote {
  id String @id @default(uuid())

  ////////////////////////////////////////////////////
  // RELATIONS
  ////////////////////////////////////////////////////
  businessId String
  customerId String?
  invoiceId  String? // nullable (credit may exist without invoice)
  vendorId   String? // for bill credits
  business   Business  @relation(fields: [businessId], references: [id])
  customer   Customer? @relation(fields: [customerId], references: [id])
  invoice    Invoice?  @relation(fields: [invoiceId], references: [id])
  vendor     Vendor?   @relation(fields: [vendorId], references: [id])

  salesReturnId String?
  salesReturn   SalesReturn? @relation(fields: [salesReturnId], references: [id])

  ////////////////////////////////////////////////////
  // CREDIT DATA
  ////////////////////////////////////////////////////
  creditNumber    String
  type            CreditNoteType // 🔥 ADD THIS
  amount          Float
  remainingAmount Float

  reason String?
  status CreditNoteStatus @default(OPEN)
  pdfUrl String?

  isDeleted Boolean   @default(false)
  deletedAt DateTime?

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  currency     String?  @default("AED")
  exchangeRate Float?   @default(1.0)

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("CreditNoteTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("CreditNoteBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  baseCurrencyAmount    Float?

  @@index([businessId])
  @@index([customerId])
}

model PurchaseRequest {
  id            String                @id @default(uuid())
  businessId    String
  requestNumber String
  department    String?
  requesterId   String?
  status        PurchaseRequestStatus @default(DRAFT)
  notes         String?
  createdAt     DateTime              @default(now())
  updatedAt     DateTime              @updatedAt

  projectId String?
  project   Project? @relation(fields: [projectId], references: [id])

  business  Business              @relation(fields: [businessId], references: [id], onDelete: Cascade)
  requester BusinessUser?         @relation(fields: [requesterId], references: [id], onDelete: SetNull)
  items     PurchaseRequestItem[]

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("PurchaseRequestTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("PurchaseRequestBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  exchangeRate          Float?  @default(1.0)
  baseCurrencyAmount    Float?

  @@unique([businessId, requestNumber])
  @@index([businessId])
  @@map("purchase_requests")
}

model PurchaseOrder {
  id String @id @default(uuid())

  //////////////////////////////////////////////////////
  // BUSINESS
  //////////////////////////////////////////////////////
  businessId String
  business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  //////////////////////////////////////////////////////
  // BASIC
  //////////////////////////////////////////////////////
  poNumber String              @unique
  status   PurchaseOrderStatus @default(DRAFT)

  //////////////////////////////////////////////////////
  // RELATIONS
  //////////////////////////////////////////////////////
  vendorId String
  vendor   Vendor @relation(fields: [vendorId], references: [id])

  warehouseId String?
  warehouse   Warehouse? @relation(fields: [warehouseId], references: [id])

  assignedToId String?
  assignedTo   BusinessUser? @relation(fields: [assignedToId], references: [id])

  projectId String?
  project   Project? @relation(fields: [projectId], references: [id])

  //////////////////////////////////////////////////////
  // PRICING
  //////////////////////////////////////////////////////
  subtotal       Float
  tax            Float?
  discount       Float?
  totalAmount    Float
  currencyCode   String? @default("AED")
  currencySymbol String? @default("AED")

  //////////////////////////////////////////////////////
  // DATES
  //////////////////////////////////////////////////////
  orderDate            DateTime
  expectedDeliveryDate DateTime?

  //////////////////////////////////////////////////////
  // NOTES
  //////////////////////////////////////////////////////
  notes String?

  items             PurchaseOrderItem[]
  bills             Bill[]
  goodsReceiveNotes GoodsReceiveNote[]

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  exchangeRate Float?   @default(1.0)

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("PurchaseOrderTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("PurchaseOrderBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  baseCurrencyAmount    Float?

  @@index([businessId, orderDate, transactionCurrencyId])
  @@index([businessId])
}

model Bill {
  id String @id @default(uuid())

  //////////////////////////////////////////////////////
  // BUSINESS
  //////////////////////////////////////////////////////
  businessId String
  business   Business @relation(fields: [businessId], references: [id])

  //////////////////////////////////////////////////////
  // BASIC
  //////////////////////////////////////////////////////
  billNumber String
  status     BillStatus @default(UNPAID)

  //////////////////////////////////////////////////////
  // RELATIONS
  //////////////////////////////////////////////////////
  vendorId String
  vendor   Vendor @relation(fields: [vendorId], references: [id])

  purchaseOrderId String?
  purchaseOrder   PurchaseOrder? @relation(fields: [purchaseOrderId], references: [id])

  grnId String?
  grn   GoodsReceiveNote? @relation(fields: [grnId], references: [id], onDelete: SetNull)

  //////////////////////////////////////////////////////
  // PRICING
  //////////////////////////////////////////////////////
  subtotal          Float
  tax               Float?
  discount          Float?
  totalAmount       Float
  outstandingAmount Float  @default(0)

  //////////////////////////////////////////////////////
  // DATES
  //////////////////////////////////////////////////////
  billDate DateTime
  dueDate  DateTime?

  //////////////////////////////////////////////////////
  // NOTES
  //////////////////////////////////////////////////////
  notes String?

  items           BillItem[]
  payments        Payment[]
  purchaseReturns PurchaseReturn[]

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  currency     String?  @default("AED")
  exchangeRate Float?   @default(1.0)

  // Phase 1 Multi-Currency Additions
  transactionCurrency   Currency? @relation("BillTransactionCurrency", fields: [transactionCurrencyId], references: [id], onDelete: SetNull)
  transactionCurrencyId String?
  baseCurrency          Currency? @relation("BillBaseCurrency", fields: [baseCurrencyId], references: [id], onDelete: SetNull)
  baseCurrencyId        String?
  baseCurrencyAmount    Float?

  @@unique([businessId, billNumber])
  @@index([businessId, billDate, transactionCurrencyId])
  @@index([businessId])
}

model TaxRate {
  id              String           @id @default(uuid())
  taxTypeId       String
  name            String
  rate            Float
  effectiveFrom   DateTime
  effectiveTo     DateTime?
  taxRuleKey      String
  taxType         TaxType          @relation(fields: [taxTypeId], references: [id], onDelete: Cascade)
  taxTransactions TaxTransaction[]
}

model Currency {
  id             String     @id @default(uuid())
  code           String     @unique
  name           String
  symbol         String
  decimals       Int        @default(2)
  businessesBase Business[] @relation("BusinessBaseCurrency")
}

model ExchangeRate {
  id             String   @id @default(uuid())
  fromCurrencyId String
  toCurrencyId   String
  rate           Float
  effectiveDate  DateTime
  source         String?
  createdAt      DateTime @default(now())
}

model JournalEntry {
  id          String   @id @default(uuid())
  businessId  String
  accountId   String
  debit       Float    @default(0)
  credit      Float    @default(0)
  description String?
  date        DateTime @default(now())

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  account  Account  @relation(fields: [accountId], references: [id], onDelete: Cascade)

  currency     String? @default("AED")
  exchangeRate Float?  @default(1.0)

  // Phase 1 Additions
  baseDebit  Float?
  baseCredit Float?
}

model Country {
  id         String     @id @default(uuid())
  code       String     @unique
  name       String
  businesses Business[]
}

model TaxFramework {
  id         String     @id @default(uuid())
  countryId  String     @unique
  name       String
  taxTypes   TaxType[]
  businesses Business[]
}

model TaxType {
  id             String       @id @default(uuid())
  taxFrameworkId String
  name           String
  rates          TaxRate[]
  taxFramework   TaxFramework @relation(fields: [taxFrameworkId], references: [id], onDelete: Cascade)
}

model TaxRule {
  id            String  @id @default(uuid())
  businessId    String
  name          String // e.g., "UAE Standard VAT 5%"
  rate          Float // e.g., 5.0
  type          String // e.g., "VAT", "GST", "SALES_TAX"
  jurisdiction  String? // e.g., "UAE", "UK", "IN"
  isRecoverable Boolean @default(true) // For input tax

  business Business            @relation(fields: [businessId], references: [id], onDelete: Cascade)
  mappings TaxAccountMapping[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@map("tax_rules")
}

model TaxTransaction {
  id               String   @id @default(uuid())
  businessId       String
  transactionType  String
  transactionId    String
  taxRateId        String
  taxAmountTxnCcy  Float
  taxAmountBaseCcy Float
  createdAt        DateTime @default(now())

  business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  taxRate  TaxRate  @relation(fields: [taxRateId], references: [id], onDelete: Restrict)

  @@index([businessId])
  @@index([transactionType, transactionId])
}

model Account {
  id         String @id @default(uuid())
  businessId String

  name     String
  type     AccountType
  code     String?
  isActive Boolean     @default(true)

  createdAt DateTime @default(now())

  business           Business            @relation(fields: [businessId], references: [id])
  entries            JournalEntry[]
  taxAccountMappings TaxAccountMapping[]
}